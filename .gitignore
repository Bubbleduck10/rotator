# Dependencies. Only the tests need them (web3.js, to check js/solana.js against).
# The page itself has no dependencies and no build step: GitHub Pages serves
# this directory as-is.
node_modules/


# Keys must never reach a repository, public or private. No keypair belongs in
# this directory at all — this is a belt-and-braces catch, not a plan.
keys/
*keypair*.json
*-deployer.json
*-committer.json
id.json

# OS noise
.DS_Store
Thumbs.db
