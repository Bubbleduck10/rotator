{
  "name": "@callout/rotator-site",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone site for $ROTATOR: the rotation, the rounds read from the chain, and a wallet lookup",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "@solana/web3.js": "^1.99.0"
  }
}
